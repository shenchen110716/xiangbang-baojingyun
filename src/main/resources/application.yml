spring:
  application:
    name: xbb-v1
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

xbb:
  jwt:
    secret: ${JWT_SECRET:change-me-in-production-at-least-32-bytes-long}
    ttl-minutes: 120
  domains:
    identity:
      datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        username: ${DB_IDENTITY_USER:identity_user}
        password: ${DB_IDENTITY_PASSWORD:identity_pw}
      flyway:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        user: ${DB_ADMIN_USER:postgres}
        password: ${DB_ADMIN_PASSWORD:postgres}
        schemas: identity
        locations: classpath:db/migration/identity
    org:
      datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        username: ${DB_ORG_USER:org_user}
        password: ${DB_ORG_PASSWORD:org_pw}
      flyway:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        user: ${DB_ADMIN_USER:postgres}
        password: ${DB_ADMIN_PASSWORD:postgres}
        schemas: org
        locations: classpath:db/migration/org
    job:
      datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        username: ${DB_JOB_USER:job_user}
        password: ${DB_JOB_PASSWORD:job_pw}
      flyway:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        user: ${DB_ADMIN_USER:postgres}
        password: ${DB_ADMIN_PASSWORD:postgres}
        schemas: job
        locations: classpath:db/migration/job
    engagement:
      datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        username: ${DB_ENGAGEMENT_USER:engagement_user}
        password: ${DB_ENGAGEMENT_PASSWORD:engagement_pw}
      flyway:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        user: ${DB_ADMIN_USER:postgres}
        password: ${DB_ADMIN_PASSWORD:postgres}
        schemas: engagement
        locations: classpath:db/migration/engagement
    settlement:
      datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        username: ${DB_SETTLEMENT_USER:settlement_user}
        password: ${DB_SETTLEMENT_PASSWORD:settlement_pw}
      flyway:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        user: ${DB_ADMIN_USER:postgres}
        password: ${DB_ADMIN_PASSWORD:postgres}
        schemas: settlement
        locations: classpath:db/migration/settlement
    fund:
      datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        username: ${DB_FUND_USER:fund_user}
        password: ${DB_FUND_PASSWORD:fund_pw}
      flyway:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        user: ${DB_ADMIN_USER:postgres}
        password: ${DB_ADMIN_PASSWORD:postgres}
        schemas: fund
        locations: classpath:db/migration/fund
    broker:
      datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        username: ${DB_BROKER_USER:broker_user}
        password: ${DB_BROKER_PASSWORD:broker_pw}
      flyway:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        user: ${DB_ADMIN_USER:postgres}
        password: ${DB_ADMIN_PASSWORD:postgres}
        schemas: broker
        locations: classpath:db/migration/broker
    profile:
      datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        username: ${DB_PROFILE_USER:profile_user}
        password: ${DB_PROFILE_PASSWORD:profile_pw}
      flyway:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        user: ${DB_ADMIN_USER:postgres}
        password: ${DB_ADMIN_PASSWORD:postgres}
        schemas: profile
        locations: classpath:db/migration/profile
    matching:
      datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        username: ${DB_MATCHING_USER:matching_user}
        password: ${DB_MATCHING_PASSWORD:matching_pw}
      flyway:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        user: ${DB_ADMIN_USER:postgres}
        password: ${DB_ADMIN_PASSWORD:postgres}
        schemas: matching
        locations: classpath:db/migration/matching
    review:
      datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        username: ${DB_REVIEW_USER:review_user}
        password: ${DB_REVIEW_PASSWORD:review_pw}
      flyway:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        user: ${DB_ADMIN_USER:postgres}
        password: ${DB_ADMIN_PASSWORD:postgres}
        schemas: review
        locations: classpath:db/migration/review
    agreement:
      datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        username: ${DB_AGREEMENT_USER:agreement_user}
        password: ${DB_AGREEMENT_PASSWORD:agreement_pw}
      flyway:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        user: ${DB_ADMIN_USER:postgres}
        password: ${DB_ADMIN_PASSWORD:postgres}
        schemas: agreement
        locations: classpath:db/migration/agreement
    voice:
      datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        username: ${DB_VOICE_USER:voice_user}
        password: ${DB_VOICE_PASSWORD:voice_pw}
      flyway:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/xbb}
        user: ${DB_ADMIN_USER:postgres}
        password: ${DB_ADMIN_PASSWORD:postgres}
        schemas: voice
        locations: classpath:db/migration/voice
