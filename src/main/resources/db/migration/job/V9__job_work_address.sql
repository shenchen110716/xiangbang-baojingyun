-- 每个岗位可以有自己的工作地点(老板 2026-08-06)。
--
-- 灵活用工里同一家单位常常在好几个工地同时开工。只有单位注册地址的话,
-- **求职者看到的是总部地址,跑错地方** —— 而这种错只有人到了现场才发现。
--
-- 可空:老岗位没有这个信息。**不在这里抄一份单位地址** ——
-- 抄了之后单位改地址,这些岗位还留着旧的,而且分不清哪个是抄来的。
-- 为空时显示什么由展示层决定(客户端:退回单位地址)。
ALTER TABLE job.job ADD COLUMN work_address VARCHAR(200);
