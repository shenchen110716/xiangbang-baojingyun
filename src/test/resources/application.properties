# outbox 中继在测试里跑得很密:多条跨域链路已改走 outbox,中继不跑下游就永远收不到事件,
# 几乎所有 await() 都会超时。
# 需要断言"事件尚未投递"的用例(SettlementOutboxReliabilityTest)用
# @SpringBootTest(properties=...) 把它覆盖成极大值再显式驱动中继——
# 所以这个默认值必须放在优先级最低的地方,不能放 @DynamicPropertySource。
xbb.outbox.relay.interval-ms=200

# 测试里不退避:绝大多数用例关心的是"失败了还能重试",不是"等多久再重试"。
# 退避本身由 OutboxBackoffTest(纯函数)和 OutboxRetryPolicyTest(设了非零退避)覆盖。
xbb.outbox.retry.base-ms=0

# 生产配置里凭据一律不带默认值(漏配即启动失败),所以测试要自己提供。
# 这个值只在测试用,和任何环境都不共享。
xbb.jwt.secret=test-only-secret-not-used-anywhere-else-at-least-32-bytes
# 测试用 mock 通道(生产必须显式开真实实现,见 xbb.channel.mode)
xbb.channel.mode=mock
xbb.mall.voucher-secret=test-only-voucher-secret

# 参数缓存有效期。**测试里必须是 0。**
# 每个 Spring 上下文有自己的 OpsService、各有一份缓存,updateSetting 只失效本上下文那份。
# 测试用上下文 A 改参数、事件却由上下文 B 处理时,B 最多 60 秒内读到的还是旧值 ——
# 表现为 await 超时,看着像业务逻辑没生效,其实是缓存没过期。
xbb.ops.setting-cache-ttl-ms=0
